pipeline {
    agent { docker { image 'node:6.3' } }
    stages {
        stage('build') {
            steps {
            	echo 'Installed the docker.\n'
                sh 'npm --version'
            }
        }
    }
}