#!/bin/sh     
ssh -i /var/lib/jenkins/workspace/SSH/MyWebServerKeyPair.pem ec2-user@52.65.13.26 <<EOF        
 echo 'now we are finnaly into the production server!'
 sudo su
 cd /var/www/yelpCampApp
 git pull
 npm install       
 node app.js
 exit       
EOF